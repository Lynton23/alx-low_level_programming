#include "main.h"


/**
 * main - Entry point
 * Description: This is the main function used for
 * testing the positive_or_negative function.
 * It initializes an integer variable `i` to 0 and
 * then calls the `positive_or_negative` function.
 * Return: 0 (Success)
 */

int main(void)
{
int 1;
i = 0;
positive_or_negative(i);

return (0);
}

